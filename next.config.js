module.exports = {
	images: {
		domains: ["links.papareact.com", "static.dezeen.com"],
	},
	env: {
		mapbox_key: process.env.MAPBOX_KEY,
	},
};

// this file should not be used to include any private keys
